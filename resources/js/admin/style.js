import Styled from 'styled-components';

const StyledHeader = Styled.div`
    display: flex;
    align-items: center;
    padding: 14px 25px;
    background: #ffffff;
`;

export {
    StyledHeader,
};